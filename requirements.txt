# app_ai.py places st.chat_input inside `with tab5:` (an inline/nested position).
# That placement raises StreamlitAPIException on older releases, so 1.32.0 could
# never run the AI tab. 1.46.1 is what the code is built and verified against.
streamlit>=1.46.1
pandas==2.2.0
plotly==5.18.0
# Imported at module scope by app.py and app_ai.py (from st_aggrid import ...),
# so it must stay installed even though neither file renders an AgGrid yet.
# 0.3.4 declares streamlit>=0.87.0 / pandas>=1.2 — no conflict with the bump above.
streamlit-aggrid==0.3.4
requests>=2.31.0
# Imported directly by app_ai.py (`import numpy as np`). pandas already pulls
# numpy in, but declare it explicitly so the import can't break on a future
# pandas that vendors or drops it.
numpy>=1.24.2
